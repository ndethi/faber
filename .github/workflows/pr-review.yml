# DISABLED: This workflow conflicts with skill-review.yml adversarial review.
# The "resolve" mode fetches ALL comments (including Copilot/adversarial reviews),
# categorizes most as "unknown" → "defer", and spams "Deferred — requires human review"
# on every push. Re-enable only when resolve mode has proper categorization and
# a manual trigger (e.g., label or /resolve-reviews comment).
#
# name: PR Review Agent
# on:
#   pull_request:
#     types: [opened, synchronize, reopened, ready_for_review]
#
# jobs:
#   review:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       pull-requests: write
#     steps:
#       if: github.event.pull_request.labels.*.name contains 'auto-resolve'
#       uses: actions/checkout@v4
#       - name: Run PR Review Resolution
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           python skills/pr-review-resolution/scripts/pr_review_resolution.py resolve \
#             ${{ github.event.pull_request.number }} \
#             ${{ github.repository }}