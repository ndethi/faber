# Build Scripts

This directory will contain the concrete scripts used by the `build` skill, such as:

- `build.sh` – wrapper that runs the appropriate toolchain.
- `package.json` scripts (if needed).
- Any helper utilities.

For now this is a placeholder awaiting implementation details.
